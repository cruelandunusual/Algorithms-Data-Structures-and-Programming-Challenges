import { binary_to_decimal } from './binary-to-decimal_function.js';

let myString = "1101010101010010101010101010";
//console.log(binary_to_decimal(myString));
console.log(binary_to_decimal(process.argv[2])); // for command line input

